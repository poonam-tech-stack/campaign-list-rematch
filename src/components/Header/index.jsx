import React from 'react';
import './index.css';

const Header = () => (
  <header className="Header">
    <div className="flex-fixed text-center header-title">Campaigns App</div>
  </header>
);

export default Header;
